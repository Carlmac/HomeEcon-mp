<view class="container">
    <i-badge show="{{showBadge}}" count="{{badgeCount}}">
        <i-icon wx:if="{{icon}}" name="{{icon}}" size="{{iconSize}}"></i-icon>
        <view>{{text}}</view>
    </i-badge>
</view>